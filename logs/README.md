# logs

This folder is for local runtime state only.

Examples:

- `dump1090.log`
- `beast-bridge.log`
- feeder status files and PID files
- the temporary dashboard key

These files are created while the tracker is running and are intentionally ignored by Git.
